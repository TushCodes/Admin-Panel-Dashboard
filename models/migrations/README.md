# Prisma Migrate

Create development migrations with:

```bash
npm run prisma:migrate:dev
```

Commit the generated migration folders in this directory and apply them in deployment with:

```bash
npm run prisma:migrate:deploy
```
